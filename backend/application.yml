spring:
  datasource:
    url: jdbc:postgresql://3.128.248.3:5432/inpdatabase_v1
    username: postgres
    password: inpservices123
    driver-class-name: org.postgresql.Driver

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update

    show-sql: true

server:
  port: 8080

